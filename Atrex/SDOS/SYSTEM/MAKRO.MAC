PUSHREG	MACRO	B
	IRP	X,<B>
	 PUSH	X
	ENDM
	ENDM

POPREG	MACRO	B
	IRP	X,<B>
	 POP	X
	ENDM
	ENDM

;;Ÿ¡slo, var adresa ýetØzce
BIN_ASC MACRO	CISLO, RET
	LOCAL	DALSI_ZNAK
	PUSHREG	<AX, BX, CX, DX, DI>
	MOV	AX, CISLO
	MOV	CX, 5
	MOV	DI, 4
	MOV	BX, 10
DALSI_ZNAK:
	XOR	DX,DX
	DIV	BX
	OR	DL,30h
	MOV	RET[DI],DL
	DEC	DI
	LOOP	DALSI_ZNAK
	POPREG	<DI, DX, CX, BX, AX>
	ENDM
